- จงแปลง user ให้เป็น JSON และลองแปลงกลับจาก JSON และเก็บไว้ในตัวแปรชื่อ parsedUser
- เปรียบเทียบ user และ parsedUser ว่าเหมือนหรือแตกต่างกัน ถ้าแตกต่างกันสาเหตุเพระอะไร

```js
const user = {
  name: 'John Smith',
  age: 35,
  sayHi() {
    console.log(`Hi ${this.name}`);
  }
};
```
