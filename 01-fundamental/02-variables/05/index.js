(function () {
  // Insert code here
  const BRAND_NAME = 'I am a hero';
  // สามารถประกาศชื่อตัวแปรเป็นตัวพิมพ์ใหญ่ได้ แต่ไม่ควรทำ เพราะจะทำให้สับสนกับค่าคงที่ (constant) ที่มักจะใช้ตัวพิมพ์ใหญ่ในการตั้งชื่อ
  // นิยมใช้ตัวพิมพ์ใหญ่สำหรับค่าคงที่ (constant) เท่านั้น เช่น PI, MAX_VALUE เป็นต้น
})();
