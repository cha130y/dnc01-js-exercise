(function () {
  // Insert code here
  // let a = undefined;
  // let b = '';
  // let c = !b;
  // let d = Boolean(a);
  // let e = Boolean(b);
  // let f = Boolean(c);

  // console.log(a); //result : undifined
  // console.log(b); //result : '' (empty string)
  // console.log(c); //result : true => !b is true because b is an empty string which is falsy, so !b is true
  // console.log(d); //result : false => undefined is falsy, so Boolean(undefined) is false
  // console.log(e); //result : false => empty string is falsy, so Boolean('') is false
  // console.log(f); //result : true => c is true, so Boolean(true) is true
})();
